import React from 'react';

import { Containerpink } from './styles';

const MinorButton = ({children}) => {
  return (
  <Containerpink>
    {children}
  </Containerpink>);
}

export default MinorButton;